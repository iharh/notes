https://docs.docker.com/compose/how-tos/use-secrets/

services:
  myapp:
    build:
      secrets:
        - npm_token
    secrets:
      - my_secret

secrets:
  my_secret:
    file: ./my_secret.txt
  npm_token:
    environment: NPM_TOKEN
