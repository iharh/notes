samples:
    podman run \
        --rm -it \
        --uidmap="0:$(id -u user):1" \
        --uidmap="1:$(grep -Po '(?<=^user:).*$' /etc/subuid | head -1)" \
        registry.fedoraproject.org/fedora:38
